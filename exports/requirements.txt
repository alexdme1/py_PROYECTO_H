# =============================================================
# requirements.txt — Proyecto H (Túnel de Flores)
# =============================================================
# Instalar con:
#   pip install -r requirements.txt
#
# NOTA: PyTorch con CUDA debe instalarse desde el índice de PyTorch.
# Si no tienes CUDA, cambia cu130 por cpu.
# Consulta: https://pytorch.org/get-started/locally/
# =============================================================

# --- PyTorch + CUDA 13.0 (nightly recomendado para detectron2) ---
--extra-index-url https://download.pytorch.org/whl/nightly/cu130
torch
torchvision
torchaudio

# --- Detectron2 (instalar desde GitHub) ---
# pip install git+https://github.com/facebookresearch/detectron2.git

# --- Clasificación (ConvNeXt) ---
timm>=0.9

# --- Interfaz web ---
streamlit>=1.40

# --- Visión por computador ---
opencv-python>=4.8
Pillow>=9.0

# --- Utilidades ---
PyYAML>=6.0
scikit-learn>=1.3
huggingface_hub>=0.20
